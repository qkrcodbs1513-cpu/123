# v7.9

- 새아침 기본 동시 접속을 4개에서 2개로 낮춰 서버 timeout을 방지
- 브라우저 2개를 유지하면서 각 브라우저가 코트 2개씩 재사용 조회
- 코트별 일시적 접속 실패 시 1회 자동 재시도
- 일부 코트의 순간 timeout으로 빈자리가 사라졌다가 다시 생긴 것처럼 오알림되는 현상 완화
- Telegram `message is not modified` 응답을 정상 no-op으로 처리해 400 재시도 로그 제거
- 현재 빈자리 보기 버튼 및 `/slots` 기능 유지
- 연수문화공원과 달빛공원 수집 로직은 변경하지 않음
